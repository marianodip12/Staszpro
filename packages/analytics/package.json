{
  "name": "@sportiq/analytics",
  "version": "0.1.0",
  "description": "SportIQ — Analytics pipeline: season aggregations, cross-match filters, trend detection.",
  "type": "module",
  "exports": {
    ".":          "./src/index.ts",
    "./pipeline": "./src/pipeline/index.ts",
    "./hooks":    "./src/hooks/index.ts"
  },
  "scripts": {
"build": "tsc -p tsconfig.json --skipLibCheck",
    "test":      "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sportiq/core": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "typescript": "^5.6.0",
    "vitest":     "^2.1.0"
  },
  "peerDependencies": {
    "react": ">=18.0.0"
  }
}
