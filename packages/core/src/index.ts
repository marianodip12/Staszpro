/**
 * @sportiq/core — Main entry point.
 *
 * Exports platform entities (multi-tenant types) and sport-specific domains.
 * Import paths:
 *   import { Organization, Match } from '@sportiq/core'
 *   import { HandballEvent, applyFilter } from '@sportiq/core/handball'
 */

export * from './types/entities';
